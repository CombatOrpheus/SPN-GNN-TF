# spn-gnn-performance

This project is set up to use `pixi` for environment and package management.

## Setup

1.  Install `pixi` by following the instructions [here](https://pixi.sh/latest/).
2.  Run `pixi install` to create the environment and install all dependencies.
